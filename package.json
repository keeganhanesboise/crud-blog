{
    "scripts": {
        "serverInstall": "cd server && npm install",
        "clientInstall": "cd client && npm install",
        "client": "cd client && npm install && npm start",
        "server": "cd server && npm install && nodemon server",
        "start": "concurrently \"npm run client\" \"npm run server\""
    },
    "dependencies": {
        "concurrently": "^7.1.0"
    }
}
